<?php

/*
 * 內容外流的防線。
 *
 * client_pool_cap:一次頁面載入,每個等級最多送幾題到瀏覽器。
 *
 * 這個數字是在「玩得順」與「不要一次把題庫送人」之間取捨。一場遊戲通常用不到
 * 十幾題,所以 40 對玩家來說跟沒有上限一樣;但想蒐集整份題庫的人得一直重載,
 * 而且付費題目每次都要重看一支廣告才拿得到新的一批。
 *
 * 設成 0 就是不限(回到舊行為)。真的遇到有人在刷,調小即可 —— 不用改程式。
 */
return [
    'client_pool_cap' => (int) env('CONTENT_CLIENT_POOL_CAP', 24),
];
